// ── Shared constants ─────────────────────────────────────────────────

/** Default HISE REST API port. */
export const DEFAULT_HISE_PORT = 1900;

/** Default observer server port (TUI ↔ CLI push events). */
export const DEFAULT_OBSERVER_PORT = 1902;
