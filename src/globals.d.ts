// ── Build-time constants injected by esbuild ───────────────────────

/** App version from package.json, injected by esbuild define. */
declare const __APP_VERSION__: string;

/** True when building with Rezi ink-compat renderer instead of stock Ink. */
declare const __REZI_COMPAT__: boolean;
