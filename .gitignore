node_modules/
dist/

# Generated by scripts/embed-web-assets.mjs from dist/web/.
src/web/embedded-assets.ts

# HISE source — clone without --recurse-submodules for C++ reference
# git clone https://github.com/christoph-hart/HISE hise-source
hise-source/

# Debug key log (flip DEBUG_KEYS in src/tui/components/Input.tsx to enable)
debug-keys.log

# Render profiler output (--profile flag)
profile.jsonl
prerender-debug.log

# Screencasts — uncompressed .cast and preview HTML are intermediate artifacts.
# Only .tape (source) and .cast.gz (compressed) are committed.
screencasts/*.cast
screencasts/index.html
/examples/hise_project/Presets
/examples/hise_project/expansion_info.xml
